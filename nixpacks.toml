[phases.setup]
nixPkgs = ["python311", 
           "gobject-introspection",
           "pango",
           "cairo",
           "gdk-pixbuf",
           "libffi",
           "shared-mime-info"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port 8000"
